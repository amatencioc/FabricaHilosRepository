@model FabricaHilos.Models.Sire.SireMonitoreoViewModel
@{
    ViewData["Title"] = "SIRE — Monitoreo";

    string BadgeHealth(string s) => s switch
    {
        "Healthy"   => "bg-success",
        "Degraded"  => "bg-warning text-dark",
        "Unhealthy" => "bg-danger",
        _           => "bg-secondary"
    };

    string IconLog(bool exito) => exito
        ? "bi-check-circle-fill text-success"
        : "bi-x-circle-fill text-danger";

    string BadgeOp(string? op) => op switch
    {
        "AUTH"      => "bg-primary",
        "EXPORTAR"  => "bg-info text-dark",
        "TICKET"    => "bg-secondary",
        "DESCARGAR" => "bg-warning text-dark",
        "HEALTH"    => "bg-success",
        _           => "bg-light text-dark border"
    };

    string BadgeHttp(int? status) => status switch
    {
        >= 200 and < 300 => "bg-success",
        >= 400 and < 500 => "bg-warning text-dark",
        >= 500           => "bg-danger",
        _                => "bg-secondary"
    };

    var tabActivo = Model.TabActivo == "log" ? "log" : "health";
}

@section Breadcrumb {
    <li class="breadcrumb-item"><a asp-controller="Contabilidad" asp-action="Index">Contabilidad</a></li>
    <li class="breadcrumb-item"><a asp-controller="Sire" asp-action="Index">SIRE</a></li>
    <li class="breadcrumb-item active">Monitoreo</li>
}

@section Styles {
    <link rel="stylesheet" href="~/css/sire.css" asp-append-version="true" />
}

@await Html.PartialAsync("~/Views/Contabilidad/Sire/_SireNavBar.cshtml")

<div class="sire-strip">
    <i class="bi bi-activity" style="color:#90cdf4;font-size:.95rem;flex-shrink:0;"></i>
    <span class="sire-strip-title">Monitoreo SIRE</span>
    <span class="sire-strip-sep"></span>
    <span class="sire-kpi">
        <i class="bi bi-check-circle-fill" style="color:#68d391;"></i>
        <span class="sire-kpi-val">@Model.HealthOk</span><span style="opacity:.75">ok</span>
    </span>
    <span class="sire-kpi">
        <i class="bi bi-exclamation-triangle-fill" style="color:#fbd38d;"></i>
        <span class="sire-kpi-val">@Model.HealthDegraded</span><span style="opacity:.75">degradado</span>
    </span>
    <span class="sire-kpi">
        <i class="bi bi-x-circle-fill" style="color:#fc8181;"></i>
        <span class="sire-kpi-val">@Model.HealthError</span><span style="opacity:.75">error</span>
    </span>
    <span class="sire-kpi" title="Uptime sobre últimos @Model.HealthTotal health checks">
        <i class="bi bi-bar-chart-fill" style="color:#90cdf4;"></i>
        <span class="sire-kpi-val" style="@(Model.UptimePercent >= 90 ? "color:#68d391;" : Model.UptimePercent >= 70 ? "color:#fbd38d;" : "color:#fc8181;")">@Model.UptimePercent%</span>
        <span style="opacity:.75">uptime</span>
    </span>
    <span class="sire-strip-sep"></span>
    <span class="sire-kpi">
        <i class="bi bi-send-fill" style="color:#90cdf4;"></i>
        <span class="sire-kpi-val">@Model.ApiTotal</span><span style="opacity:.75">llamadas</span>
    </span>
    <span class="sire-kpi">
        <i class="bi bi-check2-all" style="color:#68d391;"></i>
        <span class="sire-kpi-val">@Model.ApiExitosos</span><span style="opacity:.75">ok</span>
    </span>
    <span class="sire-kpi">
        <i class="bi bi-x-lg" style="color:#fc8181;"></i>
        <span class="sire-kpi-val">@Model.ApiFallidos</span><span style="opacity:.75">error</span>
    </span>
    @if (Model.ApiDuracionPromMs.HasValue)
    {
        <span class="sire-kpi" title="Duración promedio de llamadas HTTP">
            <i class="bi bi-stopwatch" style="color:#b794f4;"></i>
            <span class="sire-kpi-val">@Model.ApiDuracionPromMs ms</span>
            <span style="opacity:.75">prom.</span>
        </span>
    }
</div>

<div class="row g-3 mb-3">
    <div class="col-6 col-md-3">
        <div class="sire-kpi-card ok">
            <div class="sire-kpi-card-icon ok"><i class="bi bi-heart-pulse-fill"></i></div>
            <div>
                <div class="sire-kpi-card-label">Uptime</div>
                <div class="sire-kpi-card-value">@Model.UptimePercent%</div>
                <div class="sire-kpi-card-sub">@Model.HealthOk / @Model.HealthTotal ok</div>
            </div>
        </div>
    </div>
    <div class="col-6 col-md-3">
        <div class="sire-kpi-card warn">
            <div class="sire-kpi-card-icon warn"><i class="bi bi-exclamation-triangle-fill"></i></div>
            <div>
                <div class="sire-kpi-card-label">Degradado / Error</div>
                <div class="sire-kpi-card-value">@(Model.HealthDegraded + Model.HealthError)</div>
                <div class="sire-kpi-card-sub">@Model.HealthDegraded degradado · @Model.HealthError error</div>
            </div>
        </div>
    </div>
    <div class="col-6 col-md-3">
        <div class="sire-kpi-card">
            <div class="sire-kpi-card-icon"><i class="bi bi-send-fill" style="color:var(--sire-accent);"></i></div>
            <div>
                <div class="sire-kpi-card-label">Llamadas HTTP</div>
                <div class="sire-kpi-card-value">@Model.ApiTotal</div>
                <div class="sire-kpi-card-sub">@Model.ApiExitosos ok · @Model.ApiFallidos error</div>
            </div>
        </div>
    </div>
    <div class="col-6 col-md-3">
        <div class="sire-kpi-card rce">
            <div class="sire-kpi-card-icon rce"><i class="bi bi-stopwatch-fill"></i></div>
            <div>
                <div class="sire-kpi-card-label">Duración prom.</div>
                <div class="sire-kpi-card-value">@(Model.ApiDuracionPromMs.HasValue ? $"{Model.ApiDuracionPromMs} ms" : "—")</div>
                <div class="sire-kpi-card-sub">Llamadas con datos</div>
            </div>
        </div>
    </div>
</div>

@if (!string.IsNullOrWhiteSpace(Model.FiltroJobId) || !string.IsNullOrWhiteSpace(Model.FiltroOperacion))
{
    <div class="alert alert-info d-flex align-items-center gap-2 py-2 mb-3" role="alert">
        <i class="bi bi-funnel-fill"></i>
        <span>Filtro activo:</span>
        @if (!string.IsNullOrWhiteSpace(Model.FiltroJobId))
        {
            <span class="badge bg-primary">JobId: @Model.FiltroJobId</span>
        }
        @if (!string.IsNullOrWhiteSpace(Model.FiltroOperacion))
        {
            <span class="badge bg-secondary">Operación: @Model.FiltroOperacion</span>
        }
        <a asp-action="Monitoreo" asp-route-tab="log" class="btn btn-sm btn-outline-secondary ms-auto">
            <i class="bi bi-x-circle me-1"></i>Quitar filtro
        </a>
    </div>
}

<ul class="nav nav-tabs mb-0" id="monitoreoTabs" role="tablist">
    <li class="nav-item" role="presentation">
        <button class="nav-link @(tabActivo == "health" ? "active" : "")"
                id="tab-health" data-bs-toggle="tab" data-bs-target="#pane-health"
                type="button" role="tab">
            <i class="bi bi-heart-pulse me-1"></i>
            Health Checks
            <span class="badge @(Model.HealthError > 0 ? "bg-danger" : "bg-success") ms-1">@Model.HealthTotal</span>
        </button>
    </li>
    <li class="nav-item" role="presentation">
        <button class="nav-link @(tabActivo == "log" ? "active" : "")"
                id="tab-log" data-bs-toggle="tab" data-bs-target="#pane-log"
                type="button" role="tab">
            <i class="bi bi-journal-code me-1"></i>
            Log HTTP (SUNAT)
            <span class="badge @(Model.ApiFallidos > 0 ? "bg-warning text-dark" : "bg-secondary") ms-1">@Model.ApiTotal</span>
        </button>
    </li>
</ul>

<div class="tab-content sire-card border-top-0" style="border-radius:0 0 8px 8px;">

    <div class="tab-pane fade @(tabActivo == "health" ? "show active" : "")" id="pane-health" role="tabpanel">
        <div class="sire-card-header">
            <i class="bi bi-clock-history"></i>
            <span>Registro de Health Checks</span>
            <span class="ms-auto sire-badge sire-badge-secondary">@Model.HealthTotal evento(s)</span>
        </div>
        <div class="card-body p-0">
            @if (!Model.HealthLogs.Any())
            {
                <div class="text-muted text-center py-4">
                    <i class="bi bi-inbox fs-3 d-block mb-2"></i>Sin registros de health check aún.
                </div>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table sire-table table-hover align-middle mb-0" id="tblHealth">
                        <thead>
                            <tr>
                                <th style="width:165px">Timestamp (local)</th>
                                <th style="width:110px">Estado</th>
                                <th style="width:75px" class="text-center">Auth</th>
                                <th style="width:75px" class="text-center">RVIE</th>
                                <th style="width:75px" class="text-center">RCE</th>
                                <th>Descripción</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var h in Model.HealthLogs)
                            {
                                <tr>
                                    <td class="text-nowrap small">@h.FechaUtc.ToLocalTime().ToString("dd/MM/yyyy HH:mm:ss")</td>
                                    <td><span class="badge @BadgeHealth(h.Status)">@h.Status</span></td>
                                    <td class="text-center">
                                        <i class="bi @(h.AuthOk ? "bi-check-circle-fill text-success" : "bi-x-circle-fill text-danger")"
                                           title="@(h.AuthOk ? "Token OK" : "Sin token")"></i>
                                    </td>
                                    <td class="text-center">
                                        <i class="bi @(h.RvieOk ? "bi-check-circle-fill text-success" : "bi-x-circle-fill text-danger")"
                                           title="@(h.RvieOk ? $"RVIE OK — {h.RviePeriodos} períodos" : h.RvieError ?? "Error RVIE")"></i>
                                    </td>
                                    <td class="text-center">
                                        <i class="bi @(h.RceOk ? "bi-check-circle-fill text-success" : "bi-x-circle-fill text-danger")"
                                           title="@(h.RceOk ? $"RCE OK — {h.RcePeriodos} períodos" : h.RceError ?? "Error RCE")"></i>
                                    </td>
                                    <td class="small text-muted">@h.Descripcion</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>

    <div class="tab-pane fade @(tabActivo == "log" ? "show active" : "")" id="pane-log" role="tabpanel">
        <div class="sire-card-header border-bottom-0 pb-2">
            <i class="bi bi-journal-code"></i>
            <span>Auditoría de llamadas HTTP a SUNAT</span>
            <span class="ms-auto sire-badge sire-badge-secondary">@Model.ApiTotal registro(s)</span>
        </div>
        <div class="px-3 pb-2 pt-1 border-bottom">
            <form method="get" asp-action="Monitoreo" class="row g-2 align-items-end">
                <input type="hidden" name="tab" value="log" />
                <div class="col-auto">
                    <label class="form-label small mb-1">JobId</label>
                    <input type="text" name="jobId" value="@Model.FiltroJobId"
                           class="form-control form-control-sm" placeholder="Todos" style="width:230px" />
                </div>
                <div class="col-auto">
                    <label class="form-label small mb-1">Operación</label>
                    <select name="operacion" class="form-select form-select-sm" style="width:150px">
                        <option value="">Todas</option>
                        @foreach (var op in new[] { "AUTH", "EXPORTAR", "TICKET", "DESCARGAR", "GUARDAR", "CARGAR", "COMPLETAR", "CANCEL", "ERROR" })
                        {
                            <option value="@op" selected="@(Model.FiltroOperacion == op)">@op</option>
                        }
                    </select>
                </div>
                <div class="col-auto">
                    <button type="submit" class="btn btn-sm btn-primary">
                        <i class="bi bi-funnel me-1"></i>Filtrar
                    </button>
                    @if (!string.IsNullOrWhiteSpace(Model.FiltroJobId) || !string.IsNullOrWhiteSpace(Model.FiltroOperacion))
                    {
                        <a asp-action="Monitoreo" asp-route-tab="log" class="btn btn-sm btn-outline-secondary ms-1">
                            <i class="bi bi-x-circle"></i>
                        </a>
                    }
                </div>
            </form>
        </div>
        <div class="card-body p-0">
            @if (!Model.ApiLogs.Any())
            {
                <div class="text-muted text-center py-4">
                    <i class="bi bi-inbox fs-3 d-block mb-2"></i>Sin registros de log HTTP aún.
                </div>
            }
            else
            {
                <div class="table-responsive">
                    <table class="table sire-table table-hover align-middle mb-0" id="tblLog">
                        <thead>
                            <tr>
                                <th style="width:155px">Fecha (local)</th>
                                <th style="width:100px">Operación</th>
                                <th style="width:55px" class="text-center">Método</th>
                                <th style="width:70px" class="text-center">HTTP</th>
                                <th style="width:80px" class="text-center">Duración</th>
                                <th style="width:70px" class="text-center">Resultado</th>
                                <th style="width:210px">JobId</th>
                                <th>Mensaje / Resumen</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var log in Model.ApiLogs)
                            {
                                <tr>
                                    <td class="text-nowrap small">@log.Fecha.ToLocalTime().ToString("dd/MM/yyyy HH:mm:ss")</td>
                                    <td><span class="badge @BadgeOp(log.Operacion)">@log.Operacion</span></td>
                                    <td class="text-center small">@(log.MetodoHttp ?? "—")</td>
                                    <td class="text-center">
                                        @if (log.HttpStatus.HasValue)
                                        {
                                            <span class="badge @BadgeHttp(log.HttpStatus)">@log.HttpStatus</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">—</span>
                                        }
                                    </td>
                                    <td class="text-center small text-muted">
                                        @(log.DuracionMs.HasValue ? $"{log.DuracionMs} ms" : "—")
                                    </td>
                                    <td class="text-center">
                                        <i class="bi @IconLog(log.Exito)"
                                           title="@(log.Exito ? "Exitoso" : "Fallido")"></i>
                                    </td>
                                    <td>
                                        @if (!string.IsNullOrWhiteSpace(log.JobId))
                                        {
                                            <a asp-action="Monitoreo"
                                               asp-route-jobId="@log.JobId"
                                               asp-route-tab="log"
                                               class="font-monospace small text-decoration-none"
                                               title="Filtrar por este JobId">
                                                @log.JobId[..Math.Min(log.JobId.Length, 12)]…
                                            </a>
                                        }
                                        else
                                        {
                                            <span class="text-muted small">—</span>
                                        }
                                    </td>
                                    <td class="small text-muted"
                                        style="max-width:320px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;"
                                        title="@log.Mensaje">@log.Mensaje</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
        </div>
    </div>

</div>

@section Scripts {
<script>
    document.addEventListener('DOMContentLoaded', function () {
        const tabParam = new URLSearchParams(window.location.search).get('tab');
        if (tabParam === 'log') {
            const btn = document.getElementById('tab-log');
            if (btn) bootstrap.Tab.getOrCreateInstance(btn).show();
        }
    });
</script>
}
